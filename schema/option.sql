create table option (optionid int unsigned NOT NULL auto_increment,
                     pollid int(100) NOT NULL,
                     option varchar(100) not null,
                     primary key (optionid));
